{
    "functions": {
      "api/**/*.js": {
        "runtime": "nodejs14.x",
        "maxDuration": 30
      }
    },
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/app.js" }
    ]
  }
  